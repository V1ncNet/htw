Das Programm wird mit dem gcc-Befehl 
$ gcc *.c -o phonelib
kompiliert.

Um weitere Informationen zu erhalten, kann beim Kompilieren zusätzlich -DDEBUG verwendet werden.
$ gcc -DDEBUG *.c -o phonelib
